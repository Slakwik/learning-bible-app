{
  "name": "bible-learning",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:rules": "firebase emulators:exec --only firestore --project demo-bible-classes \"node --test tests/firestore.rules.cjs\"",
    "test:invitations": "firebase emulators:exec --only firestore,auth --project demo-bible-classes \"node --test tests/invitations.integration.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.18.0",
    "firebase-tools": "^15.29.0",
    "jsdom": "^30.0.1"
  }
}
